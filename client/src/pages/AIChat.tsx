import React from 'react';
import PageHeader from '../components/PageHeader';

const AIChat: React.FC = () => {
  return (
    <div>
      <PageHeader title="AI Chat" subtitle="Conversation with your documents (placeholder)" />
      <div className="p-4 bg-gray-800 rounded">Chat interface will be added later.</div>
    </div>
  );
};

export default AIChat;
